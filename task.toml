artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Parse an Apache-style access log into a small JSON summary report."

[metadata]
category = "data_processing_and_etl"
subcategory = "text_processing"
task_objective = ["transform", "generate"]
artifact_type = ["text_or_log_file", "generated_output_artifact"]
expert_time_estimate_hours = 0.3
model_tested = "GPT-5.4"
agent_tested = "Terminus-2"
difficulty_explanation = "Parse author-provided synthetic Apache-style traffic data and compute exact summary statistics used in routine web operations and analytics work."
solution_explanation = "Read each non-empty log line, aggregate the request count, unique client IPs, and requested-path frequencies, then serialize the summary to JSON."
verification_explanation = "Three pytest tests parse /app/report.json and assert the exact value for each numbered success criterion."

[verifier]
timeout_sec = 120.0

[agent]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = true
mcp_servers = []
